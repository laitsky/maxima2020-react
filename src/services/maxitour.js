const baseUrl = 'https://laitsky.tech/assets/360/mxm_';

const gate = `${baseUrl}home_gate.png`;
const maximap = `${baseUrl}home_maximap.png`;
const mainStage1 = `${baseUrl}main_stage_1.png`;
const mainStage2 = `${baseUrl}main_stage_2.png`;
const malpunGate = `${baseUrl}malpun_gate.png`;
const photobooth = `${baseUrl}malpun_photobooth.png`;
const row12 = `${baseUrl}row_12.png`;

export default {
  gate,
  maximap,
  mainStage1,
  mainStage2,
  malpunGate,
  photobooth,
  row12,
};
